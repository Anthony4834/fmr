{
  "name": "fmr-fyi-chrome-extension",
  "version": "1.1.1",
  "description": "Chrome extension for FMR.fyi",
  "scripts": {
    "build": "bun run build:ts && bun run build:bundle",
    "build:ts": "tsc",
    "build:bundle": "bunx webpack --mode production",
    "dev": "bunx webpack --mode development --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.11.0",
    "copy-webpack-plugin": "^12.0.2",
    "ts-loader": "^9.5.1",
    "typescript": "^5.3.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}
