# Copy this to .env and fill in your actual values

# Database
POSTGRES_URL=postgresql://user:password@host:port/database

# Upstash Redis for rate limiting
# Get these from your Upstash dashboard: https://console.upstash.com/
# Create a Redis database and copy the REST URL and Token
# Note: KV_REST_API_URL and KV_REST_API_TOKEN are the same as Upstash REST credentials
KV_REST_API_URL=https://your-redis-instance.upstash.io
KV_REST_API_TOKEN=your-upstash-redis-token

# ============================================
# Authentication (NextAuth.js)
# ============================================

# NextAuth.js secret for JWT signing
# REQUIRED - Generate with: openssl rand -hex 32
NEXTAUTH_SECRET=generate-a-32-byte-hex-secret-here

# Base URL for NextAuth callbacks (required for OAuth)
# Set to your deployed URL in production
NEXTAUTH_URL=http://localhost:3000

# OAuth token encryption key (AES-256 requires 32 bytes = 64 hex characters)
# REQUIRED - Generate with: openssl rand -hex 32
TOKEN_ENCRYPTION_KEY=generate-a-32-byte-hex-key-here

# ============================================
# OAuth Providers
# ============================================

# Google OAuth
# Get from: https://console.cloud.google.com/apis/credentials
# Add callback URL: https://yourdomain.com/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret


# ============================================
# Auth Security Settings (optional)
# ============================================

# Maximum failed login attempts before account lockout (default: 5)
AUTH_MAX_FAILED_ATTEMPTS=5

# Account lockout duration in minutes (default: 15)
AUTH_LOCKOUT_DURATION_MINUTES=15

# Window for counting failed attempts in minutes (default: 60)
AUTH_ATTEMPT_WINDOW_MINUTES=60

# Admin secret for promoting users to admin role
# REQUIRED - Generate with: openssl rand -hex 32
ADMIN_SECRET=generate-a-random-secret-here
