'use client';

/**
 * Returns the number of geos on the user's shortlist.
 * Stub: always returns 0. Later can merge localStorage and/or API.
 */
export function useShortlistCount(): number {
  return 0;
}
